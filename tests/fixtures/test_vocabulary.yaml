# ============================================================
# Minimal vocabulary fixture used by tests.
# ============================================================

owner:
  nik: Nikil
  lab: Lab

provider:
  hom: Home Lab
  aws: AWS

environment:
  prd: Production
  dev: Development

resource_type:
  vm: Virtual Machine
  ct: Container

purpose:
  infrastructure:
    core: Core
    backup: Backup
  applications:
    blog: Blog

tags:
  ownership:
    - owner
    - team
  network:
    - hostname
    - ipv4
